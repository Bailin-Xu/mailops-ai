generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Domain models will be introduced with the EML import vertical slice.
// Keeping this schema empty avoids committing speculative tables before the
// choices in docs/03-domain-model.md are resolved.
